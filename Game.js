import wonders from './wonders'

export default class Game {
  constructor ({
    decks,
    players,
  } = {}) {
    this.decks = decks
    this.players = players
  }

  static generatePlayers (n) {

  }
}
